<% provide :panel_title, 'Announcements list' %>

<% content_for :actions_list do %>
  <li><%= link_to 'New Announcement', new_school_announcement_path(@school) %></li>

  <li class="divider"></li>
  <li><%= link_to 'School', @school %></li>
<% end %>

<%= render layout: 'wide_panel' do %>
  <table class="table">
    <thead>
      <tr>
	<th>Title</th>
	<th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @announcements.each do |announcement| %>
	<tr>
	  <td><%= link_to announcement.title, announcement %></td>
	  <td><%= discuss_link announcement %></td>
          <td><%= link_to 'Edit', edit_announcement_path(announcement) %></td>
          <td><%= link_to 'Destroy', announcement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	</tr>
      <% end %>
    </tbody>
  </table>
<% end %>
