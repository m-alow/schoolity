<% provide :panel_title, 'Exams' %>

<% content_for :actions_list do %>
  <li><%= link_to 'Teacher Panel', teacher_path %></li>
<% end %>

<%= render layout: 'simple_panel' do %>
  <div class="row">
    <div class="col-xs-7">
      <ul class="nav nav-stacked">
	<% @classrooms.each do |c| %>
	  <li><%= link_to qualified_classroom_name(c), classroom_exams_path(c) %></li>
	<% end %>
      </ul>
    </div>

    <div class="col-xs-5">
      <ul class="nav nav-stacked">
	<% @classrooms.each do |c| %>
	  <li><%= link_to 'New Exam', new_classroom_exam_path(c) %></li>
	<% end %>
      </ul>
    </div>
  </div>
<% end %>
