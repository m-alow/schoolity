$("#classroom-<%= classroom_id %>-lesson-<%= lesson.order %>")
  .html("<%= j render('lessons/content', lesson: lesson) %>")
  .effect('highlight', 1500)
  .append("<%= j render('lessons/activities_links', lesson: lesson) %>");
