<p id="notice"><%= notice %></p>

<h1>Listing School Classes Announcements</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @school_classes_announcements.each do |school_classes_announcement| %>
      <tr>
        <td><%= link_to 'Show', school_classes_announcement %></td>
        <td><%= link_to 'Edit', edit_school_classes_announcement_path(school_classes_announcement) %></td>
        <td><%= link_to 'Destroy', school_classes_announcement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Announcement', new_school_classes_announcement_path %>
