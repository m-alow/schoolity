$("#comments").append('<li id="comment-<%= comment.id %>" class="list-group-item"><%= j render('comments/comment', comment: comment) %></li>');
$("#comment-<%= comment.id %>").effect("highlight", 1500);
$("#comment-form").html("<%= j render('comments/form', commentable: comment.commentable, comment: Comment.new) %>");
