<%= simple_form_for [commentable, comment], remote: true do |f| %>
  <div class="form-inputs">
    <%= f.input :body, placeholder: 'Write a comment...', label: false %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Comment' %>
  </div>
<% end %>
