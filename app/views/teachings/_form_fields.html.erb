<%= f.error_notification %>

<div class="form-inputs">
  <%= f.association :teacher, as: :email, label: 'Email' %>
  <%= f.association :subject, collection: @classroom.school_class.subjects %>
  <%= f.input :all_subjects %>
</div>

<div class="form-actions">
  <%= f.button :submit %>
</div>
